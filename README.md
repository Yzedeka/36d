# 36d

36D is a lossless text compression and encoding library using DEFLATE/Brotli/Zstd when available, wrapped in a custom Base-76 representation and split into 36 deterministic dimensions.

## Install

```bash
npm install 36d